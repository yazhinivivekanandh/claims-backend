# Copy to .env and fill server-side values only. Never commit real secrets.
# Leave values empty to use the development defaults in app/config.py.

API_TOKEN=
DATABASE_PATH=
APP_HOST=
APP_PORT=

# Integration stage (populated later, never share these values in chat):
YOXA_TRIGGER_URL=
YOXA_SECRET=
